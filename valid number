class Solution {
    public boolean isNumber(String s) {
        // Regular expression to validate a number as per the given rules
        String regex = "^[+-]?((\\d+\\.?\\d*)|(\\.\\d+))([eE][+-]?\\d+)?$";
        
        // Return whether the input matches the regular expression
        return s.matches(regex);
    }
}
